# EXP 17: BINARY PHASE SHIFT KEYING (BPSK)
## OBJECTIVES
> 1. To generate a Binary Phase Shift Keying (BPSK) signal.
> 2. To observe the 180° phase reversal corresponding to digital logic transitions.'
> 3. To demodulate a BPSK signal using a product detector.
> 4. To restore the recovered digital signal using a comparator.
> 5.  To understand the relationship between BPSK and DSBSC modulation.
## MATERIALS
> 1. Emona Telecoms-Trainer 101 (plus power-pack)
> 2. Dual-channel 20MHz oscilloscope
> 3. Master Signals, Sequence Generator, Multiplier (Product Modulator), Tuneable Low-pass Filter, Comparator (Utilities), Variable DCV
> 4. Three oscilloscope leads and assorted patch leads
## INTRODUCTION
>Binary Phase Shift Keying (BPSK) is a digital modulation technique in which the phase of a carrier signal is changed according to the binary input data, while the amplitude and frequency remain constant. In BPSK, two distinct phase states are used to represent the binary digits 0 and 1. Typically, a phase of 0° represents binary “1”, while a phase shift of 180° represents binary “0.” This means that the carrier signal reverses its phase whenever the digital data changes from one binary state to another.
>In a BPSK modulation system, the digital input signal is used to control the phase of the carrier wave generated by an oscillator. When the input bit is 1, the carrier is transmitted with its normal phase, and when the input bit is 0, the carrier is shifted by 180 degrees. The resulting modulated signal appears as a sinusoidal waveform that flips its phase depending on the input data sequence. At the receiver, a BPSK demodulator or phase detector is used to compare the received signal with a reference carrier. By detecting the phase differences, the system can determine whether the transmitted bit is a 0 or a 1 and recover the original digital information.

# PROCEDURES
## PART A: GENERATING A BSPK SIGNAL
> 1. Gather a set of the equipment listed above.
> 2. Set up the scope per the instructions in Experiment 1.
> 3. Set the scope's Trigger Source control to the EXT position.
> 4. Set the scope's Trigger Source Coupling control to the HF REJ position.
> 5. Set the scope's Channel 1 and Channel 2 Input Coupling controls to the DC position.
> 6. Set the scope's Timebase control to the 0.1ms/div position.
> 7. Locate the Sequence Generator module and set its dip-switches to 00.
> 8. Connect the set-up so that:
        > Sequence Generator CLK connects to Master Signals 8kHz clock.
        > Sequence Generator SYNC connects to Scope EXT trigger.
        > Sequence Generator OUT connects to Multiplier X input.
        > Master Signals 100kHz sine connects to Multiplier Y input.
